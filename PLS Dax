
1. RLS set at PageLevelSecruity Table where you have a list of pages with different users accesss/view that is True/False
2. Build relationship with PageLevelSecruity Table Access with List of Page (Filter Selection)
3. Build normal RLS with userprincipalname
4. Build RLS + PLS as DAX below

var __PLSAccess = 
    CALCULATE(COUNT('AccessTable'[Email]),
    FILTER('AccessTable','AccessTable'[Email] = USERPRINCIPALNAME() && 'AccessTable'[PLS Access] = "FullAccess"))

RETURN 
If(__PLSAccess > 0, [FullAccess] == TRUE(), [PartiallyAccess] == TRUE())
